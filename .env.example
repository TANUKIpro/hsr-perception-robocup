# =============================================================================
# HSR Perception Pipeline - Environment Variables
# Copy this file to .env and modify as needed
# =============================================================================

# -----------------------------------------------------------------------------
# ROS2 Configuration
# -----------------------------------------------------------------------------
# Domain ID for ROS2 DDS communication (0-232)
# All nodes must use the same domain ID to communicate
ROS_DOMAIN_ID=0

# -----------------------------------------------------------------------------
# Display Configuration
# -----------------------------------------------------------------------------
# X11 display for GUI applications (visualization, etc.)
DISPLAY=:0

# -----------------------------------------------------------------------------
# Data Directories
# -----------------------------------------------------------------------------
# Paths can be absolute or relative to docker-compose.yml location
# Use absolute paths for external storage (e.g., NVMe, external HDD)

# Dataset storage (images, annotations)
DATASETS_DIR=./datasets

# Model storage (pretrained, finetuned)
MODELS_DIR=./models

# User profiles (competition-specific settings)
PROFILES_DIR=./profiles

# Training run outputs (logs, checkpoints)
RUNS_DIR=./runs

# -----------------------------------------------------------------------------
# GPU Configuration (optional)
# -----------------------------------------------------------------------------
# Limit which GPUs are visible to the container
# CUDA_VISIBLE_DEVICES=0

# -----------------------------------------------------------------------------
# Performance Tuning (optional)
# -----------------------------------------------------------------------------
# Shared memory size for PyTorch DataLoader
# Increase if you see "RuntimeError: unable to write to file </torch_xxx>"
# SHM_SIZE=8gb
