# HSR Perception Pipeline - Python Dependencies
# Training + evaluation over YOLO datasets produced by pybullet_hsr.

# Core ML / CV
# torch / torchvision are installed separately in the Dockerfile from the
# CUDA 12.1 wheel index. Do NOT list them here — pip would re-resolve
# against the default PyPI index and overwrite the cu121 build with a
# newer CUDA wheel incompatible with the host driver.
ultralytics>=8.3.0
opencv-python>=4.8.0
numpy>=1.24.0
pillow>=10.0.0

# Utilities
pyyaml>=6.0
tqdm>=4.66.0
colorama>=0.4.6
tabulate>=0.9.0
pandas>=2.0.0

# Visualization
matplotlib>=3.7.0
tensorboard>=2.14.0
plotly>=5.0.0

# Web UI
streamlit>=1.37.0

# Development & Testing
pytest>=7.4.0
pytest-cov>=4.1.0
black>=23.0.0
isort>=5.12.0
mypy>=1.5.0
